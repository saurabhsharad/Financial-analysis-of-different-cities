# Financial Predictor Frontend

React + Vite frontend for the financial vulnerability predictor.

## Run locally

Start the Flask backend from the project root:

```bash
python api.py
```

Then start the frontend from this folder:

```bash
npm install
npm run dev
```

The app sends predictions to `http://localhost:5000/predict`.
